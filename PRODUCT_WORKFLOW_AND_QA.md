# 일정 운영 화면: 제품 설계·구현·QA 기준

이 문서는 일정 운영 화면을 변경할 때 따르는 기본 작업 순서와 이번 개편의 확정 요구사항, QA 기준을 한 곳에 기록합니다. 기능을 먼저 만들고 나중에 화면을 맞추지 않습니다.

## 기본 작업 순서

1. **문제와 사용자 목표 정의**: 누가 어떤 판단·행동을 가장 자주 하는지, 현재의 혼란과 실패 지점을 기록한다.
2. **정보 구조(IA)와 화면 흐름 설계**: 정보 중요도·관련도에 따라 화면 영역, 기본 선택 상태, 주 행동과 보조 행동을 정한다.
3. **기능·데이터 요구사항 확정**: 필수 입력, 유효성 검사, 상태 변화, 오류·빈 상태, 권한을 명시한다.
4. **수용 기준과 QA 케이스 작성**: 구현 전 자동·수동·E2E 검증 항목과 기대 결과를 적는다.
5. **구현**: 작은 컴포넌트 단위로 개발하며, 접근성·반응형·로딩/오류 상태를 함께 만든다.
6. **검증**: 타입 검사, 린트, 자동 테스트, 실제 API E2E, 데스크톱·모바일 시각 QA를 순서대로 실행한다.
7. **결과 기록과 배포**: 발견 결함·수정·남은 위험을 문서화하고, 검증된 커밋만 main 및 원격 저장소에 반영한다.

## 이번 개편의 사용자 목표

관리자는 한 화면에서 “언제 일정이 있는지 → 그날 무엇을 운영하는지 → 어느 일정의 세션과 강사 지원을 조정할지”를 빠르게 판단해야 합니다. 강한 색상·중복 필터·테이블 중심의 정보 나열은 이 흐름을 방해하므로 제거합니다.

## 정보 구조(IA)

| 우선순위 | 영역 | 사용자 질문 | 화면/행동 |
| --- | --- | --- | --- |
| 1 | 달력 탐색 | 언제 무엇을 처리해야 하나? | 화면 상단의 월간 달력, 오늘·선택 날짜·일정 건수 표시 |
| 2 | 오늘·선택 날짜의 일정 | 그날 무엇을 운영해야 하나? | 달력 바로 아래의 날짜, 건수, 모집 중 수, 일정 행 |
| 3 | 일정 상세 | 이 일정의 조건과 현재 상태는? | 선택한 행 바로 아래의 인라인 상세, 상태·교육 유형·필요 인원 |
| 4 | 세션과 강사 지원 | 어떤 시간에 누구를 배정할까? | 상세 안의 세션 운영과 접수 순서 지원 관리 |
| 5 | 찾기·필터·계정 | 목록을 좁히거나 계정을 관리할까? | 운영 목록 상단의 단일 검색/필터 바, 헤더 보조 행동 |

데스크톱과 모바일 모두 `상단 달력 → 선택 날짜 운영 목록 → 선택 행의 인라인 상세` 순서입니다. 상세는 선택한 일정 바로 아래에 이어 붙여 문맥을 유지합니다. 모바일에서는 행의 열 제목을 숨기고 요약 정보로 바꾸며 가로 스크롤을 만들지 않습니다.

## 기능·디자인 요구사항

### 1. 일정 운영 메인

- 헤더는 서비스 제목, 한 줄 설명, 알림, 로그아웃만 둔다. 새 일정 등록은 사용 맥락인 운영 목록의 제목 옆 주요 버튼으로 둔다.
- 달력 날짜에는 일정 유무뿐 아니라 **건수**를 표시하고, 오늘·선택 날짜·선택 날짜의 일정 유무를 서로 구별한다. 달력은 모든 화면 폭에서 운영 목록보다 먼저 둔다.
- 운영 목록은 선택 날짜를 기본값으로 사용하며, 기관/지역 검색, 교육 유형, 모집 상태를 한 번씩만 제공한다. 날짜 필터와 같은 의미의 중복 필터는 제공하지 않는다.
- 일정 행은 기관명·지역, 교육 유형, 모집 정원, 모집 상태를 같은 열에 정렬한다. 기관명은 가장 크고, 정원 열은 오른쪽 정렬하며, 상태는 색 점과 텍스트를 함께 쓴다. 주 행동은 행 전체의 상세 열기, 수정·삭제는 보조 행동이다.
- 빈 상태에는 “선택한 날짜에 일정이 없습니다”와 `새 일정 등록` 경로를 함께 제공한다.

### 2. 일정 등록·수정

- 화면 위에 떠 있는 대화상자로 열리며, 배경의 운영 문맥을 잃지 않는다.
- 필수값은 업무 날짜, 기관명, 지역, 필요 배정 인원, 교육 유형, 모집 상태다.
- 필요 배정 인원에는 “최종 승인 가능한 강사 수”라는 설명을 표시하고 1명 이상을 즉시 검증한다.
- 저장 성공 시 대화상자를 닫고 해당 날짜를 선택해 새 카드를 즉시 보여 준다.

### 3. 일정 상세와 세션 운영

- 상세 패널은 선택 행 바로 아래에 인라인으로 열며, 기관명, 날짜, 지역, 교육 유형, 모집 상태, 필요 배정 인원을 레이블/값의 고정 열로 보여 주고 수정 행동을 제공한다. 같은 행을 다시 누르면 닫히고, 다른 행을 누르면 이전 상세는 닫힌다.
- 세션 목록은 표가 아니라 시간·세션명/모집 담당자·유형·메모·관리 행동을 읽기 쉬운 카드로 표시한다.
- 승인 현황은 `승인 인원 / 필요 인원`으로 보이며, 지원자는 접수 순서·지원 시각·상태·배정/반려/재검토 행동을 제공한다.

### 4. 새 세션 등록·수정

- 세션 등록은 선택한 일정의 상세 안에서 열리는 대화상자다. 상단에 대상 날짜와 기관을 고정 표시한다.
- 필수값은 시작 시각, 종료 시각, 세션명 또는 모집 담당자(현 데이터 필드 `instructor`), 교육 유형이다. 메모는 선택값이다.
- 종료 시각은 시작 시각보다 늦어야 하며, 현지 브라우저 시간대 기준임을 명시한다. 서버의 ISO 오프셋 검증도 통과해야 저장된다.
- 저장 성공 뒤 목록을 새로 고치고, 실패 시 입력을 보존한 채 오류를 알려 준다.

### 5. 접근성·반응형

- 모든 주요 행동은 키보드로 초점 이동·실행할 수 있고, 대화상자는 `role="dialog"`·제목 연결·배경 닫기·취소 버튼을 제공한다.
- 버튼과 입력은 최소 44px 터치 영역을 유지한다.
- 320px부터 한 열 배치, 카드형 세션/지원 목록, 줄바꿈 가능한 행동 영역을 제공한다.

## QA 케이스와 수용 기준

| ID | 구분 | 절차 | 기대 결과 |
| --- | --- | --- | --- |
| IA-01 | 메인 | 관리자 로그인 | 상단 달력과 그 아래 선택 날짜 운영 목록이 보이고, 새 일정 등록은 운영 목록 제목 옆에 있다. |
| IA-02 | 탐색 | 일정이 있는 날짜와 없는 날짜를 각각 선택 | 선택 날짜 강조와 해당 날짜의 정확한 일정 건수·목록이 갱신된다. |
| IA-03 | 필터 | 기관/지역 검색, 유형, 상태를 조합 후 초기화 | 조건에 맞는 카드만 보이고 초기화 시 선택 날짜 전체가 복원된다. |
| SCH-01 | 일정 등록 | 모든 필수값을 입력해 저장 | 저장 직후 해당 날짜가 선택되고 카드가 보인다. |
| SCH-02 | 일정 검증 | 필요 인원 0, 필수값 누락 | 저장되지 않고 한국어 오류가 입력 근처에 표시된다. |
| SCH-03 | 일정 상세 | 일정 행 선택 | 해당 행 바로 아래에 상세 요약과 세션·지원 영역이 열리고, 한 번에 하나만 열린다. |
| SES-01 | 세션 등록 | 새 세션 등록에서 정상 시간·필수값 저장 | 대상 일정에 세션 카드가 즉시 추가된다. |
| SES-02 | 세션 검증 | 종료 시각을 시작보다 이르게 입력 | 저장되지 않고 명확한 오류가 표시된다. |
| APP-01 | 지원/승인 | 강사 지원 후 관리자가 배정 | 접수 순서 목록, 승인 상태, 승인 인원, 강사 확정 알림이 모두 갱신된다. |
| APP-02 | 제약 | 정원 초과 또는 겹치는 시간의 강사 배정 시도 | 저장되지 않고 원인을 한국어로 안내한다. |
| A11Y-01 | 키보드 | Tab/Enter/Escape와 대화상자 배경 클릭 | 초점 표시가 보이고, 취소/배경 클릭으로 안전하게 닫힌다. |
| RWD-01 | 모바일 | 320px·375px·768px 폭 확인 | 한 열 우선순위, 버튼 줄바꿈, 가로 잘림 없음, 핵심 행동 44px 이상을 만족한다. |
| REG-01 | 회귀 | 타입 검사·린트·자동 테스트·API E2E 실행 | 모두 통과하며 기존 신청→승인→알림 흐름이 유지된다. |

## 이번 실행 결과

2026-09-14에 아래 검증을 완료했습니다.

- 자동 검증: 타입 검사·프런트/백엔드 린트·프로덕션 빌드 통과, 프런트 3 suite/6 tests 통과, 백엔드 12 suite/32 tests 통과.
- 실제 API E2E: 강사 가입·로그인 → 관리자 일정/세션 생성 → 모집 마감/재개 → 2명 접수 순서 확인 → 관리자 승인 → 강사 내역·확정 알림·읽음 처리 → 정원 초과 차단 → 동시간 배정 충돌 차단 → 대기 지원 취소·승인 지원 취소 차단을 통과했다. E2E 중 관리자 라우트가 강사 라우트를 가로막는 결함을 발견해 경로별 권한 검사로 수정했다.
- 브라우저 QA(관리자): 일정 등록 뒤 선택 날짜로 즉시 이동·카드/달력 건수 갱신(SCH-01), 상세·세션 운영 표시(SCH-03), 세션 등록(SES-01), 종료 시각 역전 오류(SES-02), 기관 검색과 초기화(IA-03), 대화상자 닫기를 확인했다.
- 반응형 시각 QA: 375×812 및 320×720 뷰포트에서 한 열 우선순위, 전체 폭 등록 버튼·필터, 카드형 일정/세션 표시와 버튼 줄바꿈을 확인했다. 검증 뒤 뷰포트는 기본값으로 복원했다.
- QA 정리: 브라우저 검증을 위해 만든 `UI QA 기관` 및 `QA 확인 기관` 일정·연결 세션 2건을 삭제했다. 사용자 일정은 변경하지 않았다.
- 2026-09-15 재설계: Astra의 정보 구조 검토와 캘린더 운영 화면 레퍼런스를 반영해 [DESIGN_DIRECTION_AND_WIREFRAME.md](DESIGN_DIRECTION_AND_WIREFRAME.md)를 갱신했다. 달력을 화면 상단으로 이동하고, 목록을 정렬된 행 구조로 바꾸며, 오른쪽 드로어를 선택 행 바로 아래의 인라인 상세로 교체했다. 이 변경에 대한 데스크톱·모바일 시각 QA 결과는 구현 검증 뒤 이 문서에 추가한다.
