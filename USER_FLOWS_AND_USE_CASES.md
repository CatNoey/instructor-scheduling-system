# 서비스 운영 시나리오와 제품 명세

이 문서는 납품·개발·QA가 같은 기준으로 쓰는 제품 명세입니다. 기능을 구현하기 전에 이 문서의 **운영 시나리오 → 하이레벨 스펙 → 세부 스펙 → 수용 기준** 순서로 합의합니다.

화면 구조는 [DESIGN_DIRECTION_AND_WIREFRAME.md](DESIGN_DIRECTION_AND_WIREFRAME.md), 구현·QA 절차는 [PRODUCT_WORKFLOW_AND_QA.md](PRODUCT_WORKFLOW_AND_QA.md), AWS 배포는 [AWS_DEPLOYMENT_GUIDE.md](AWS_DEPLOYMENT_GUIDE.md)를 참조합니다.

## 1. 운영 시나리오

### SC-01. 최초 도입: 관리자 주도 계정 일괄 등록

1. 관리자가 엑셀 파일을 업로드한다.
2. 시스템의 LLM 보조 기능이 헤더와 행을 읽고 `성명·연락처·주소·자격 티어`에 대응하는 열을 제안한다.
3. 누락값, 중복 연락처, 해석하기 어려운 열, 잘못된 자격 티어를 미리보기에서 표시한다.
4. LLM은 관리자에게 누락된 값 또는 매핑 선택을 질문한다. 답변 전에는 실제 계정을 만들지 않는다.
5. 관리자가 정제된 미리보기, 생성·수정·제외 건수와 경고를 검토하고 **최종 확인**한다.
6. 시스템이 계정과 사용자 프로필을 한 번의 등록 작업으로 생성하고, 결과 및 제외 사유를 보관한다.

### SC-02. 상시 운영: 일정 생성부터 강사 최종 배정까지

1. 관리자가 상단 월간 캘린더에서 날짜를 선택하고 일정·세션을 등록한다.
2. 강사가 모집 중인 세션에 지원한다.
3. 관리자는 접수 순서, 자격 티어, 기존 승인 시간대를 확인한다.
4. 관리자가 승인·반려·재검토를 수동으로 결정한다.
5. 승인 시 정원·시간 충돌을 다시 검증하고, 성공한 강사에게 브라우저 내 확정 알림을 생성한다.
6. 모든 승인 상태 변경과 삭제/복구는 영구 이력에 남는다.

### SC-03. 상시 운영: 사용자 추가·수정

1. 관리자는 사용자 관리 화면에서 한 명을 직접 등록하거나 수정한다.
2. 여러 명이면 엑셀 업로드를 다시 사용한다. 기존 연락처와 일치하는 사용자는 신규 생성 대신 수정 후보로 보여 준다.
3. 관리자가 변경 내역을 확인하고 저장한다.
4. 사용자 프로필 및 계정 상태 변경은 영구 이력에 남는다.

### SC-04. 운영 조정: 예외 발생 처리

1. 정원 초과, 동일 시간 충돌, 모집 마감, 사용자 정보 누락 등의 예외가 발생한다.
2. 시스템은 자동으로 승인하거나 임의 조정하지 않고, 원인과 가능한 다음 행동을 한국어로 안내한다.
3. 관리자는 필요 배정 인원·모집 상태·세션 시간·지원 상태를 수동으로 조정한다.
4. 조정 전후 값과 실행자는 이력에 남는다.

## 2. 하이레벨 스펙 요약

| 영역 | 목표 | 핵심 기능 | 확정 정책 |
| --- | --- | --- | --- |
| 계정·사용자 | 관리자가 강사 풀을 신뢰성 있게 관리 | 엑셀+LLM 초기/벌크 등록, 직접 등록·수정, 사용자 목록 | 관리자만 계정 발급·수정. 사용자 기본 정보는 성명·연락처·주소·자격 티어 |
| 일정 운영 | 날짜별 운영 현황을 빠르게 판단 | 월간 캘린더, 일정·세션 CRUD, 모집 상태 | 캘린더 → 날짜 목록 → 인라인 상세 흐름 |
| 지원·배정 | 공정한 접수와 안전한 수동 승인 | 세션 지원, 접수 순서, 승인·반려·재검토 | 동일 시간 충돌은 선착순, 최종 조정은 관리자 수동 결정 |
| 알림 | 확정 결과를 즉시 전달 | 로그인 후 브라우저 내 알림·읽음 처리 | 이메일·SMS·외부 푸시 미사용 |
| 이력·감사 | 납품 후 운영 변경을 추적 | 삭제/복구, 승인 변경, 벌크 등록 작업 이력 | 보존 기간은 영구 |
| 목록·검색 | 데이터가 많아도 빠르게 찾기 | 서버 검색, 정렬, 페이지네이션 | 기본 50개 단위, 서버에서 필터·검색 적용 |
| 시간 | 국내 단일 운영 기준 유지 | 날짜·세션 시간 입력과 검증 | 조직 고정 시간대·해외 일정·시간대 변환은 범위 제외 |

## 3. 세부 스펙

### 3.1 계정 발급과 사용자 관리

#### 사용자 데이터 모델

| 필드 | 형식/예시 | 규칙 |
| --- | --- | --- |
| 사용자 ID | 시스템 생성 숫자 ID | 수정 불가, 외부 표시 최소화 |
| 성명 | `홍길동` | 최종 등록 시 필수 |
| 연락처 | `010-1234-5678` | 최종 등록 시 필수. 숫자 정규화 후 중복을 검사 |
| 주소 | `서울시 …` | 선택 입력. 빈 값은 LLM 미리보기에서 확인 대상으로 표시 |
| 자격 티어 | `T1`, `T2`, `T3` 등 고객사 정의값 | 최종 등록 시 필수. 허용 티어 목록 밖의 값은 확인 필요 |
| 역할 | 관리자 / 강사 | 엑셀 기본값은 강사. 관리자 역할 부여는 별도 권한 확인 필요 |
| 계정 상태 | 활성 / 비활성 / 보류 | 비활성 사용자는 로그인·신규 지원 불가, 이력은 유지 |

자격 티어의 실제 코드와 표시명은 고객사별 설정값으로 관리한다. 초기 기본값을 임의로 고정하지 않는다.

#### 엑셀 + LLM 등록 플로우

```text
엑셀 업로드
  → 파일 구조·행 수·암호화 여부 검사
  → LLM 열 매핑 제안 및 값 정규화 제안
  → 관리자 질의(누락/모호/중복/티어 불일치)
  → 정제 미리보기(생성·수정·제외·오류 건수)
  → 관리자 최종 확인
  → 트랜잭션 등록 또는 수정
  → 결과 요약·행별 오류 파일·감사 이력 저장
```

| 단계 | 상세 요구사항 |
| --- | --- |
| 파일 입력 | `.xlsx`를 우선 지원한다. 파일 크기·행 수 제한은 인프라 부하 시험 후 설정하며, 제한 초과 시 분할 업로드를 안내한다. |
| 열 해석 | LLM은 헤더·샘플 행을 바탕으로 매핑만 제안한다. 성명/연락처/주소/자격 티어를 확정값으로 추정해 바로 저장하지 않는다. |
| 관리자 질의 | 필수 필드 누락, 다수 후보 열, 중복 연락처, 알 수 없는 티어, 형식 오류가 있으면 질문 목록으로 표시한다. |
| 미리보기 | 원본 행 번호, 해석된 값, 생성/수정/제외 예정 상태, 오류·경고를 모두 표시한다. |
| 최종 확인 | 관리자가 명시적으로 확인하기 전에는 DB 쓰기·계정 생성·기존 사용자 수정이 발생하지 않는다. |
| 중복 | 정규화된 연락처가 일치하면 기존 사용자 수정 후보로 표시한다. 자동 덮어쓰기는 하지 않으며 관리자 선택이 필요하다. |
| 결과 | 성공·수정·제외·실패 건수와 행별 사유를 내려받을 수 있는 결과로 제공한다. |
| 보안 | 엑셀과 LLM 질의에는 개인정보가 포함될 수 있다. 운영 배포 전 승인된 LLM 제공자, 데이터 보관 기간, 접근 제어, 마스킹 정책을 별도 계약/보안 검토로 확정한다. |

#### 직접 등록·수정

- 관리자는 사용자 목록에서 `사용자 추가`로 한 명을 입력할 수 있다.
- 사용자 목록은 성명, 연락처, 주소, 자격 티어, 역할, 계정 상태를 테이블로 보여 준다.
- 성명·연락처·자격 티어는 저장 전 검증한다. 주소가 없으면 경고 후 저장은 허용한다.
- 연락처 중복, 자격 티어 미등록, 비활성 사용자에 대한 신규 지원은 명확한 오류로 안내한다.
- 사용자 삭제는 기본적으로 소프트 삭제로 처리한다. 개인정보 법령에 따른 별도 파기 요청은 영구 감사 이력 정책과 충돌할 수 있으므로 고객사의 법무·개인정보 정책을 우선한다.

### 3.2 일정·세션 운영

| 기능 | 상세 요구사항 |
| --- | --- |
| 일정 | 업무 날짜, 기관명, 지역, 필요 배정 인원, 교육 유형, 모집 상태를 관리한다. 필요 배정 인원은 일정 전체에서 승인 가능한 강사 수다. |
| 탐색 | 모든 화면에서 상단 월간 캘린더를 먼저 보여 주고, 선택 날짜의 일정 목록을 그 아래에 표시한다. |
| 상세 | 일정 행을 선택하면 바로 다음에 인라인 상세를 연다. 한 번에 하나만 열리고 같은 행을 다시 선택하면 닫힌다. |
| 세션 | 일정 안에 시작/종료 시각, 세션 표시명, 교육 유형, 메모를 등록한다. 종료 시각은 시작 시각보다 반드시 늦어야 한다. |
| 모집 상태 | `모집 중`, `마감`, `조정 필요`를 관리자가 수동으로 변경한다. 시스템이 자동 마감·자동 재개하지 않는다. |
| 시간 정책 | 국내 단일 운영을 가정한다. 서버는 명시적 오프셋이 있는 시각을 저장하며, 조직 고정 시간대·해외 일정·시간대 변환은 제공하지 않는다. |

### 3.3 강사 지원·관리자 승인

| 기능 | 상세 요구사항 |
| --- | --- |
| 지원 | 활성 강사는 모집 중인 세션에 한 번만 지원할 수 있다. 지원 시각을 기록한다. |
| 검토 순서 | 관리자는 기본적으로 지원 시각 오름차순(선착순)으로 본다. 자격 티어는 판단 보조 정보이며 자동 우선순위 규칙은 적용하지 않는다. |
| 승인 | 관리자는 지원을 승인, 반려, 대기로 재검토할 수 있다. 최종 결정은 자동화하지 않는다. |
| 정원 검증 | 승인 직전에 해당 일정의 승인 인원이 필요 배정 인원보다 작은지 트랜잭션으로 다시 검사한다. |
| 시간 충돌 | 승인 직전에 같은 강사의 이미 승인된 세션과 시간이 겹치는지 검사한다. 충돌 시 먼저 승인된 배정을 유지하고 새 승인을 거부한다. |
| 취소 | 강사는 대기 상태의 본인 지원만 취소할 수 있다. 승인·반려된 지원은 관리자가 상태를 변경해야 한다. |
| 알림 | 승인 성공 시 해당 강사 계정에 브라우저 내 확정 알림을 생성한다. 알림은 로그인한 본인만 조회·읽음 처리한다. |

### 3.4 영구 이력과 감사

| 이벤트 | 반드시 남길 정보 |
| --- | --- |
| 일정/세션 생성·수정·삭제·복구 | 대상 ID, 이전값, 이후값, 실행자, 시각, 사유(있는 경우) |
| 지원 생성·취소 | 지원자, 세션, 상태, 실행자, 시각 |
| 승인 상태 변경 | 이전 상태, 이후 상태, 정원/충돌 검사 결과, 실행자, 시각 |
| 사용자 생성·수정·비활성·복구 | 대상 사용자, 이전값, 이후값, 실행자, 시각 |
| 벌크 업로드 | 원본 파일 식별자, 매핑/질의 응답, 생성·수정·제외 건수, 최종 확인자, 행별 결과 |

- 감사 이력은 수정·삭제할 수 없는 추가 전용(append-only) 저장 구조를 목표로 한다.
- 보존 기간은 **영구**다. 백업과 복구 절차에도 이력을 포함한다.
- 개인정보 파기 의무가 발생하면 법적 요구를 우선하며, 이 경우 이력에는 원문 대신 비식별 참조값과 파기 사유만 보존하는 정책을 고객사와 확정한다.

### 3.5 목록, 검색, 페이지네이션

| 목록 | 기본 정렬 | 서버 검색 범위 | 페이지 정책 |
| --- | --- | --- | --- |
| 사용자 | 성명 오름차순 | 성명, 정규화 연락처, 주소, 자격 티어, 역할, 상태 | 기본 50개, 다음/이전 페이지, 최대 100개 요청 |
| 일정 | 업무 날짜 내림차순, 기관명 오름차순 | 기관명, 지역, 업무 날짜, 교육 유형, 모집 상태 | 날짜 목록은 기본 50개. 달력 건수는 별도 집계 |
| 세션 | 시작 시각 오름차순 | 세션 표시명, 기관명, 교육 유형, 업무 날짜 | 일정 상세 안은 50개 단위 |
| 지원 | 접수 시각 오름차순 | 강사 성명, 연락처, 자격 티어, 상태, 세션/기관명 | 세션별 기본 50개 |
| 감사 이력 | 발생 시각 내림차순 | 대상 종류/ID, 실행자, 이벤트 종류, 기간 | 기본 50개, 기간 필터 필수 |

- 모든 검색·필터·정렬은 서버에서 수행한다. 클라이언트가 전체 대량 데이터를 미리 받지 않는다.
- API는 `pageSize` 기본값 50, 최대값 100을 적용한다. 결과에는 현재 페이지, 다음 페이지 여부, 전체 건수 또는 안전한 추정치를 포함한다.
- 연락처는 권한 있는 관리자에게만 전체 표시한다. 목록에서는 기본 마스킹(`010-****-5678`)을 적용하고 상세/업로드 검토에서만 필요한 범위로 노출한다.

### 3.6 브라우저 알림과 권한

- 알림 채널은 로그인 후 서비스 화면에서 보는 브라우저 내 알림으로 한정한다.
- 이메일, SMS, 카카오 알림톡, 운영체제 푸시 알림은 범위에 포함하지 않는다.
- 알림은 승인 확정, 운영상 필수 상태 변경 등으로 생성하며 읽음 여부를 보관한다.
- 관리자 권한은 사용자·일정·세션·지원·감사 이력 관리에 필요하다. 강사는 본인 프로필, 본인 지원, 본인 알림에만 접근한다.

## 4. 상태 전이와 제약

```text
일정: 모집 중 ↔ 마감
                 ↕
              조정 필요       (모두 관리자 수동 변경)

지원: 대기 ── 관리자 승인 ─→ 승인 ── 관리자 재검토 ─→ 대기
  │             │
  │             └─ 브라우저 내 확정 알림 생성
  ├─ 강사 취소 (대기 상태만 가능)
  └─ 관리자 반려 ───────────→ 반려 ── 관리자 재검토 ─→ 대기
```

| 제약 | 시스템 동작 |
| --- | --- |
| 일정 정원 도달 | 추가 승인을 거부하고 관리자에게 수동 조정 경로를 안내 |
| 같은 강사의 시간 충돌 | 이미 승인된 더 이른 배정을 유지하고 새 승인을 거부 |
| 동일 세션 중복 지원 | 지원 생성을 거부하고 기존 지원 상태를 안내 |
| 마감/조정 필요 일정 지원 | 신규 지원을 거부하고 현재 모집 상태를 안내 |
| 대기 외 지원 취소 | 취소를 거부하고 관리자 재검토가 필요함을 안내 |
| 업로드 미확정 | 미리보기·질의 단계에서는 DB를 변경하지 않음 |

## 5. 수용 기준과 범위 외 항목

### 수용 기준

| ID | 검증 대상 | 완료 기준 |
| --- | --- | --- |
| ACC-01 | 벌크 업로드 | 누락·중복·모호한 매핑을 관리자가 해결하고 최종 확인 전에는 계정이 생성되지 않는다. |
| ACC-02 | 사용자 관리 | 관리자가 성명·연락처·주소·자격 티어를 목록에서 검색하고 50개 단위로 탐색할 수 있다. |
| ACC-03 | 일정 운영 | 캘린더 → 날짜 목록 → 인라인 상세 흐름에서 일정과 세션을 관리할 수 있다. |
| ACC-04 | 승인 제약 | 정원 초과와 시간 충돌을 동시 요청에도 안전하게 차단한다. |
| ACC-05 | 알림 | 승인된 강사만 자신의 브라우저 내 확정 알림을 확인하고 읽음 처리할 수 있다. |
| ACC-06 | 감사 | 삭제와 승인 변경, 벌크 업로드의 실행자·시각·전후 값을 영구 조회할 수 있다. |
| ACC-07 | 개인정보 | 연락처는 목록 기본 마스킹, 업로드 결과와 감사 이력은 관리자 권한으로 제한한다. |

### 범위 외 또는 별도 결정 항목

- 조직 고정 시간대, 해외 일정, 시간대 자동 변환
- 이메일·SMS·외부 푸시 알림
- 자동 승인·자격 티어 기반 자동 우선순위
- 공개 가입, 초대 링크, SSO 로그인
- 자격 티어 코드 체계와 표시명
- 개인정보 파기·법적 보존 충돌 시의 고객사 정책
- LLM 제공자, 개인정보 처리 계약, 파일·질의 보관 정책

이 항목들은 실제 구현 전에 별도 보안·법무·운영 결정을 거쳐야 한다. 현재 서비스의 자동화 범위는 관리자의 판단을 보조하고, 최종 계정 등록·배정·조정은 관리자가 확정하는 수준으로 제한한다.
